#include <stdio.h>

/**
 * main - prints program name
 *
 * @argc: number of arg
 * @argv: array of arg
 *
 * Return: Always 0
 */

int main(int argc, char const *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
