#include <stdio.h>
#include "main.h"
/**
 * _abs - print the absolute value of an int
 * @n : is the int who will get the absolute of it
 *
 * Return: the absolute value of int
 *
 */

int _abs(int n);
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
