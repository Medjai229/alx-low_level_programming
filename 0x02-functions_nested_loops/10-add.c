#include <stdio.h>
#include "main.h"
/**
 * add - adds two int and returns the result
 * @a: int added to b
 * @b: int added to a
 *
 * Return: sum of a and b
 *
 */

int add(int a, int b)
{
	int x;
	x = a + b;

	return (x);
}
